services:
  piaware:
    image: ghcr.io/sdr-enthusiasts/docker-piaware:latest
    container_name: piaware
    restart: unless-stopped
    network_mode: host
    environment:
      - TZ=America/New_York
      - RECEIVER_TYPE=relay
      - BEASTHOST=127.0.0.1
      - BEASTPORT=30005
      # - FEEDER_ID=YOUR_FEEDER_ID  # Set after first run
      - ALLOW_MLAT=yes
      - MLAT_RESULTS=yes
      - MLAT_RESULTS_BEASTHOST=127.0.0.1
      - MLAT_RESULTS_BEASTPORT=30104
    volumes:
      - /var/cache/piaware:/var/cache/piaware

  opensky:
    image: ghcr.io/sdr-enthusiasts/docker-opensky-network:latest
    container_name: opensky
    restart: unless-stopped
    ports:
      - "8093:80"
    environment:
      - TZ=America/New_York
      - BEASTHOST=host.docker.internal
      - BEASTPORT=30005
      - LAT=${OPENSKY_LAT:-39.09725}
      - LONG=${OPENSKY_LONG:--77.01173}
      - ALT=${OPENSKY_ALT:-46}
      - OPENSKY_USERNAME=${OPENSKY_USERNAME:-}
      - OPENSKY_SERIAL=${OPENSKY_SERIAL:-}
      - DISABLE_DUMP1090=yes
    extra_hosts:
      - "host.docker.internal:host-gateway"
    tmpfs:
      - /run:exec,size=64M
      - /var/log
